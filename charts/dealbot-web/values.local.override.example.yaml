# Copy this file to values.local.override.yaml (gitignored) and customize as needed.
# This file is intended for non-secret, local-only overrides.
#
# Example: force the browser to call the backend directly (instead of the in-cluster /api* proxy):
# env:
#   VITE_API_BASE_URL: "http://localhost:8080"
