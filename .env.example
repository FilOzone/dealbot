## Root local development secrets
#
# This file is used by `make secret` to create a Kubernetes Secret for local Kind dev.
# Keep this file limited to *secrets only* so it doesn't accidentally override Helm values
# (non-secret configuration lives in the Helm `env:` ConfigMap values).
#
# Required
WALLET_ADDRESS=
WALLET_PRIVATE_KEY=
#
# Optional (only if using an external DB or a non-default password)
# DATABASE_PASSWORD=
